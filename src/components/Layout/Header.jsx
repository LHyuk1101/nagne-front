import {AppBar} from "@mui/material";

const Header = () => {
    return (
        <AppBar position="relative" style={{marginBottom: '5rem'}}>
            1
            2
            3
            4

        </AppBar>
    );
}

export default Header;